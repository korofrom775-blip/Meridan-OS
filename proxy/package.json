{
  "name": "meridian-proxy",
  "version": "1.0.0",
  "description": "Reverse proxy for Meridian OS browser — strips X-Frame-Options so any site loads in the iframe.",
  "main": "proxy.js",
  "scripts": {
    "start": "node proxy.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=16"
  }
}
